// Carousel Auto-Cycle for team captians  $(document).ready(function() {    $('.carousel').carousel({      interval: 6000    })  });  // header change properties on  scroll $(window).scroll(function () {    if ($(this).scrollTop() > 100) {        $("header").addClass("fixed-theme");        $('.bg-header-internal').css('visibility', 'hidden')    }    else {        $("header").removeClass("fixed-theme");         $('.bg-header-internal').css('visibility', '')    }});